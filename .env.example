# .env.example
# Optional: choose LLM backend; defaults are local Ollama/Mistral
MODEL_PROVIDER=ollama
MODEL_NAME=mistral
# Optional: if someone wants OpenAI later (your code doesn’t require it now)
# OPENAI_API_KEY=sk-your-key-here